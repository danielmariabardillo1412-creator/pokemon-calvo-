name: TEMP Trainer AI C3e notebook append

on:
  push:
    branches:
      - audit/trainer-ai-c3e-readiness-contract-doc-staging

permissions:
  contents: write

jobs:
  append-c3e-doc:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: audit/trainer-ai-c3e-readiness-contract-doc-staging
      - name: Append C3e checkpoint and self-clean
        shell: bash
        run: |
          set -euo pipefail
          cat .audit/trainer_ai_c3e_readiness_contract.md >> docs/project_book/TRAINER_AI.md
          rm .audit/trainer_ai_c3e_readiness_contract.md
          rm .github/workflows/trainer-ai-c3e-doc-append-temporary.yml
          git diff --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add docs/project_book/TRAINER_AI.md .audit/trainer_ai_c3e_readiness_contract.md .github/workflows/trainer-ai-c3e-doc-append-temporary.yml
          git diff --cached --check
          git commit -m 'docs(trainer-ai): record C3e readiness contract audit'
          git push origin HEAD:audit/trainer-ai-c3e-readiness-contract-doc-staging
