name: TEMP Trainer AI C3d notebook append

on:
  push:
    branches:
      - audit/trainer-ai-c3d-doc-staging

permissions:
  contents: write

jobs:
  append-c3d-doc:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: audit/trainer-ai-c3d-doc-staging
      - name: Append C3d checkpoint and self-clean
        shell: bash
        run: |
          set -euo pipefail
          cat docs/project_book/_C3D_APPEND_TEMP.md >> docs/project_book/TRAINER_AI.md
          rm docs/project_book/_C3D_APPEND_TEMP.md
          rm .github/workflows/trainer-ai-c3d-doc-append-temporary.yml
          git diff --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add docs/project_book/TRAINER_AI.md docs/project_book/_C3D_APPEND_TEMP.md .github/workflows/trainer-ai-c3d-doc-append-temporary.yml
          git commit -m 'docs(trainer-ai): record certified C3d production boundary'
          git push origin HEAD:audit/trainer-ai-c3d-doc-staging
